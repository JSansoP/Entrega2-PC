procedure Main is

begin
   --  Insert code here.
   null;
   BABUINS : constant integer := 10; --N/2 babuins a cada costat
   MAX_COUNT : constant integer := 3; --Nombre de vegades que cada babui cruça

   monitor : NSMonitor;

   task type Babui is
end Main;
